# Copy to .env and fill in real values. Never commit .env.
POSTHOG_API_KEY=
POSTHOG_HOST=https://app.posthog.com
OPENAI_API_KEY=
GITHUB_PAT=
GITHUB_REPO=your-username/your-repo
# Secret used to verify incoming webhook requests (generate with: openssl rand -hex 32)
WEBHOOK_SECRET=
